{
  "name": "cross-verify-cli",
  "version": "0.1.0",
  "description": "Pre-publish verification CLI: a deterministic gate (secrets, PII, license) plus an independent second-model review (any verifier command) — so the model that wrote the code is never the one that approves it.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "cross-verify": "src/cli.js"
  },
  "scripts": {
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/oh-namgyu/cross-verify-cli.git"
  },
  "keywords": [
    "ai-agents",
    "code-review",
    "verification",
    "secrets-scan",
    "llm",
    "pre-commit",
    "open-source-release"
  ],
  "dependencies": {
    "commander": "^13.0.0"
  }
}
